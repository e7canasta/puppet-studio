[project]
name = "simula-geometry"
version = "0.1.0"
description = "Geometry and 2.5D lifting utilities for Simula"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
